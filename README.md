# qa_python_sprint_4

# В файле tests.py собраны тесты для методов класса BooksCollector:

# Пример теста от Практикума: изменена строка "assert len(collector.get_books_rating()) == 2" на "assert len(collector.get_books_genre()) == 2", т.к. в классе BooksCollector не существует метода get_books_rating

# 1. Тестирование метода add_new_book с параметризацией: книги с невалидной длиной наименования не добавляются словарь

# 2. Тестирование метода set_book_genre: жанр не из списка genre не устанавливается для книг

# 3. Тестирование метода get_book_genre: возвращается жанр существующей книги

# 4. Тестирование метода get_books_with_specific_genre: возвращаются все книги указанного жанра

# 5. Тестирование метода get_books_genre: для нового объекта класса возвращается пустой словарь

# 6. Тестирование метода get_books_for_children: книги с возрастным рейтингом не возвращаются

# 7. Тестирование метода add_book_in_favorites: существующая книга добавляется в избранное

# 8. Тестирование метода delete_book_from_favorites: существующая книга удаляется из избранного

# 9. Тестирование метода get_list_of_favorites_books: возвращается список избранных книг